# Optional local database used by the legacy application.
DB_HOST=127.0.0.1
DB_NAME=specialty_food_store
DB_USER=showcase_user
DB_PASSWORD=change-me

# External integrations remain disabled by code in this public showcase.
SMS_API_URL=https://example.invalid/disabled
SMS_API_KEY=not-configured
BACKUP_PASSWORD=change-me
